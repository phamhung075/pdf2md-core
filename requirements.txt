# markdown-extract-service — Complete dependencies
#
# Heavy ML/OCR engine dependencies (cached in Docker base layer)
# Includes: docling[rapidocr]==2.125.0, pdf-oxide>=0.3.70, pypdf>=5.0.0
-r requirements-base.txt

# Model Context Protocol (MCP) server SDK for agentic AI tools
mcp>=1.2.0

# Production SaaS Web Gateway & Decoupled Compute Pipeline (Step 1)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
celery>=5.4.0
redis>=5.0.0
